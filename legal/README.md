# OMNIX Legal PDF Builder

Generates a single PDF containing the current Terms of Service and Privacy Policy.

## Usage

```bash
python scripts/build_legal_pdf.py
```

The script uses ReportLab. Install it with `pip install reportlab` if it is not already present.

The output is `legal/OMNIX-Legal-Policies.pdf`.
