name: Apply Production Hardening

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  harden:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply deterministic hardening
        run: python scripts/production_hardening.py
      - name: Validate Python syntax
        run: python -m compileall -q main.py backend
      - name: Validate frontend build
        run: |
          npm ci --ignore-scripts
          npm run build
      - name: Commit hardening changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add main.py src/components/ui/HomeFeed.tsx
          git diff --cached --quiet || git commit -m 'feat: production performance and request hardening'
          git push
